info:
  name: Cancelar Aula
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/aulas/4/cancelar
  body:
    type: json
    data: |-
      {
        "motivo": "Aluno não conseguiu comparecer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

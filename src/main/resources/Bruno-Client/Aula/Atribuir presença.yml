info:
  name: Atribuir presença
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/aulas/4/presenca
  body:
    type: json
    data: |-
      {
        "presenca": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

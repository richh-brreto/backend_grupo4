info:
  name: Listar Professor ID
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/professores/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Listar Professores
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/professores
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

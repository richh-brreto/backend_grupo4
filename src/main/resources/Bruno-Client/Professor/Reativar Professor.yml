info:
  name: Reativar Professor
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:8080/professores/2/reativar
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

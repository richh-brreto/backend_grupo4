info:
  name: Cadastrar Professor
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/professores
  body:
    type: json
    data: |-
      {
        "nome": "Ricardo",
        "email": "ricasrdo@email.com",
        "telefone": 119999999,
        "senha": "senha123",
        "idTipoProfessor": 1
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/login
  body:
    type: json
    data: |-
      {
        "email": "admin@sptech.school",
        "senha": "admin123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
